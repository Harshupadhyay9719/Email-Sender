{
  "name": "email-sender",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "start": "npm --prefix backend start",
    "build": "node -e \"const cp=require('child_process'); if (process.env.RENDER) { cp.execSync('npm install --production=false', {stdio:'inherit'}); } cp.execSync('npm --prefix backend run build && npm --prefix frontend run build', {stdio:'inherit'});\"",
    "start:backend": "npm --prefix backend start",
    "start:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev"
  }
}
